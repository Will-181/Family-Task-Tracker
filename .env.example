# Database (Vercel Postgres or Neon)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=require
# - PORT must be a number (e.g. 5432). No spaces or extra characters.
# - If PASSWORD contains special characters (@ # : / % etc.), URL-encode them
#   (e.g. @ -> %40, # -> %23, : -> %3A, / -> %2F).
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require"

# Passcode gate: set a shared passcode for family access
FAMILY_PASSCODE="your-secure-passcode"

# Signing secret for the access cookie (use a long random string)
COOKIE_SECRET="your-long-random-secret-at-least-32-chars"
